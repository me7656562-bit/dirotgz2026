{
  "buildCommand": "node scripts/clean-migrations.js && prisma db push --schema=./prisma/schema.prisma --accept-data-loss --skip-generate && prisma generate --schema=./prisma/schema.prisma && next build"
}
